(defcfg
  process-unmapped-keys no
  danger-enable-cmd no
)

(defsrc
  caps
)

(defvar
  tap-time 200
  hold-time 150
)

(defalias
  caps (tap-hold-release $tap-time $hold-time esc lctl)
)

(deflayer base
  @caps
)
